{
 "_comment": "v16: v15 king model/sampler/budget + reinstated Muon optimizer tuning (WSD 1-sqrt anneal, decoupled Muon weight decay, faster embedding LR). v15 shipped bare cosine / no WD / embed at max_lr; the recipe flags all three as crown levers and the v0.3.5 lineage used them. 5150st/2.70B tok, fits the 5.0 H100h budget.",
 "vocab_size": 50257,
 "dim": 1024,
 "n_layers": 16,
 "n_heads": 16,
 "head_dim": 64,
 "ffn_mult": 2.6667,
 "max_seq_len": 512,
 "seq_len": 512,
 "batch_size": 1024,
 "micro_batch_size": 128,
 "total_steps": 5150,
 "warmup_steps": 150,
 "max_lr": 0.003,
 "min_lr": 3e-05,
 "schedule": "wsd",
 "stable_frac": 0.6,
 "decay_frac": 0.4,
 "decay_curve": "1-sqrt",
 "optimizer": "muon",
 "muon_lr": 0.025,
 "muon_momentum": 0.95,
 "muon_weight_decay": 0.03,
 "embed_optimizer": "adamw",
 "embed_lr": 0.006,
 "log_every": 50
}
