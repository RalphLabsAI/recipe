{
  "_comment": "Untied-embeddings + torch.compile fast pipeline-test config. Derived from h100_proxy (254M, seq 512). total_steps is intentionally small for a quick end-to-end submission test; the resulting checkpoint is UNDERTRAINED and will not beat the king. total_steps here MUST equal the steps actually run (integrity gate check_recipe_config_matches_proof). For a real crown attempt, raise total_steps back to full scale (e.g. 2400+) and keep compile=true.",
  "vocab_size": 50257,
  "dim": 1024,
  "n_layers": 16,
  "n_heads": 16,
  "head_dim": 64,
  "ffn_mult": 2.6875,
  "max_seq_len": 1024,
  "seq_len": 512,
  "batch_size": 512,
  "micro_batch_size": 128,
  "total_steps": 60,
  "warmup_steps": 6,
  "max_lr": 0.0008,
  "min_lr": 3e-05,
  "weight_decay": 0.1,
  "beta1": 0.9,
  "beta2": 0.98,
  "grad_clip": 1.0,
  "log_every": 10,
  "tie_embeddings": false,
  "compile": true
}
